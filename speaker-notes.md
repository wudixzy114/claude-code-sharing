# Claude Code 分享讲稿

时长：45 分钟左右（讲 38 分钟，留几分钟一起聊）
场合：周会轮流分享，同事之间交流。
一句话目标：听完你能解答两类疑惑——一是它的底层机制到底怎么回事，二是我怎么把它用得更好。所以全程围着我们平时用它、心里没底的几个问题来讲。

用法说明：每页右下角有个 N 键可以调出本页讲稿。第 02、07、10、16 页有可以点的动画，讲的时候直接点，别念屏幕上的字。第 10 页压缩动画有两个模式——"压一步"手动走微压缩→全量两类做法、"看动态压缩"也是手动一步步点着看 autoCompact 何时触发升级，都点一下。第 16 页权限动画：改左边模式 / 规则集，右边七条命令的判定一起实时重算。

数字说明：全程出现的占比、token 量都是"实测示例，随版本 / 配置变，现场可以 `/context` 查"，别当成死数报出去。

---

## 封面（1.5 分钟）

大家好。今天不背功能列表，也不逐行读源码。咱们换个角度：我们几乎每天都在用 Claude Code——它读代码、改文件、跑命令，一个任务很长它也能接着做。我一直好奇，它背后到底做了哪些事，才能"一直干活、还不闯祸"。

今天围着四个我们自己都会犯嘀咕的问题来讲：循环这一圈里它到底替我们干了啥、它凭什么能一直聊下去、这轮授的权换个对话还算不算数、Skill 和 MCP 到底怎么最快接进来。讲完你能解答两类疑惑：底层机制，和怎么用得更好。

全程用一个例子串起来：假设我让它"重构 parsePath 这个函数，然后跑测试证明没改坏"。

---
# 一、循环与配置
---

## 01 架构总览：先说结论 + 五层地图（3 分钟）

先把最核心的一句话摆出来：模型本身只会做一件事——看现在手里有什么信息，然后说下一步该干嘛，或者说"我要调某个工具"。就这么多。真正把这句话变成动作、再把结果喂回去的，是模型外面那层运行时：存状态、执行工具、处理报错、管上下文和权限。所以你让它重构 parsePath，你感觉它在"连续干活"，其实是这个圈在转。别把 Agent 理解成一个更大更聪明的 Prompt，它是一个受约束的圈。

再给一张五层地图定位，不用记模块名：你从哪进来（命令行、IDE、SDK、网页）；界面和会话；中间是核心那个循环；下面是各种工具；横着贯穿的是上下文、配置、权限、扩展。关键区别：会话状态跨很多轮一直存着（消息、用量、你拒过的操作、缓存），单次调用只管当前这一轮——分开了，中途压缩、失败重试、开子任务才不会把整盘搞乱。

## 02 循环动画（2.5 分钟）—— 现场点

这是全场的地基。我不念字，直接点给大家看。（点"下一步"）

左边这个圈会一步步高亮，右边同时在长出一条条消息——这就是它的"上下文"。第一轮：读函数。第二轮：改写。第三轮：跑测试——（点到失败那步）挂了一个，空 query 返回 null。注意：测试失败被当报错整理好、回到下一轮，模型补了个分支，没崩。第四轮全过，它就停。

最想让大家记住的是右边这坨：模型每一轮看到的，都是这份越来越长的记录。所以下一节问题就来了——这份记录越长越占地方，怎么办？

## 03 分支与恢复：功夫全在岔路上（3 分钟）

很多人以为它里面有棵特别复杂的决策树。其实主干朴素到你会有点失望：准备、问一次、跑完塞回去。真正花功夫的是四条岔路，源码里每条都有明确的转移类型——Continue（本轮没完、带状态接着转）或 Terminal（就地收尾）。①接着做：模型要调工具，跑完带进下一轮（next_turn）。②拐回来：上下文撑爆(413)，先塌缩排干、还不行触发一次反应式压缩再重试。③纠偏：一批工具里某个 Bash 挂了，只砍掉同批兄弟、本轮照常继续。④停下来：你取消 / 预算用光 / 修不了的错，落进终止态。

右下角那条 8 级恢复链是它出错时按顺序试的自救梯子，从流式回退一路到预算续跑，实在不行才把错误交还给你。调工具要过一串关（认出→校验→查权限→真跑→收拾结果），其中"收拾结果"决定上下文会不会被撑爆——细节留到记忆章。落脚：可靠 = 错误落进看得见、能恢复、能停下的状态，这就是 while(true) 和真能干活的循环的区别。

## 04 每轮上下文管理链（2.5 分钟）—— NEW

上一页"准备"那步不是一下子完成的，是一条固定顺序的预处理管线，每问一次模型前都会走，从便宜、不丢信息，逐级到贵、开始丢信息。六步：①从上次压缩边界起算，只带边界之后的消息；②给工具结果的 token 花销设上限，大日志先在这压住；③按需局部裁历史（命中 HISTORY_SNIP 门控才动）；④增量微压缩，一点点合并旧内容；⑤上下文塌缩（命中 CONTEXT_COLLAPSE 门控），把尾端旧结果换引用；⑥兜底整段自动压缩，最贵、最后才触发，就是上一页那条拐回来岔路真正调模型的地方。

关键：这六步你完全看不见；偏用户视角、能手动 /compact 的那套四层压缩细节留到记忆章讲，这里只讲顺序和位置。诚实边界：顺序取自逆向文档，逐步的数值阈值文档没给。

## 05 配置读哪些、谁盖谁、要不要重启（3 分钟）

这页回答三个最容易踩坑的问题。左边 settings.json 五级优先级，高到低：企业 managed > 命令行参数 > 项目本地 settings.local.json > 项目共享 settings.json > 用户 ~/.claude。关键一句：权限规则走"合并"不走"覆盖"——高优先级不会把低优先级整份盖掉，各层 allow/ask/deny 叠加生效。

右边讲改完要不要重启：大多数**实时热更新**（权限、hook 改完即生效），**例外是 model 和 outputStyle 要重启**。别把该热更新的当成要重启，也别以为高优先级能一键盖掉一切。拿不准就 /permissions 看当前生效哪条。（这条优先级和"合并不覆盖"，权限章第 15 页会再用到。）

## 06 每一轮该听谁的（2.5 分钟）

压缩管的是"历史放不下"，这页管的是"每一轮到底该听谁的"。别把项目指令写成一大段长 Prompt。更好的做法是当成有优先级的配置：组织红线谁都不能改、系统能力、项目规矩、个人习惯，分清楚。这也解释了：同一句话在不同仓库表现不一样，往往不是提示词不够长，是它没分清哪条更硬、来自哪。

落地模板：项目指令短而硬——怎么构建、怎么验证、哪些目录别碰、交付给什么证据；会变的进度放会话摘要。

---
# 二、记忆（样板章，最重点）
---

## 07 上下文里到底装了啥（3 分钟）—— 现场点

第二个专题，回答"凭什么能一直聊"。先破误解：它靠的不是无限大的窗口，而是把有限窗口当资源来调度。

（点"填充看看"，一块块填）这次不只分"看得见 / 看不见"，而要看它从哪来、什么时候进入：系统规则、内置工具定义、CLAUDE.md 是会话启动时由 Claude Code 注入；人的输入和模型回复是对话逐轮产生；工具结果是执行后由运行时动态回填；最后一块只是为下一次输出预留的空间。

橙色那块"工具结果"独占约 38%，是最容易把目标挤出去的一块：一次构建日志、一次搜索、一个大文件就可能撑爆。图例现在按"启动注入 / 对话产生 / 动态回填 / 输出预留"区分，现场对着 /context 就能验证实际占用。

## 08 一轮上下文按什么顺序组装（3 分钟）

第 07 页讲"装了啥"，这页回答"谁放进去、什么时候放"。按一轮的时间线分三段。

第一段是**会话启动时的稳定前缀**：系统提示词、内置工具 schema、CLAUDE.md（祖先目录）、auto memory、每个 Skill 的名字 + description、MCP 工具名和环境信息。它们在你输入前就存在；后续请求通常复用 prompt cache，但不能理解成只发送一次后就永远消失。

第二段是**人发起这一轮**：当前任务、补充要求、粘贴内容、附件，以及人在 IDE 中显式选中的代码。第三段才是**运行时按需补齐**：system-reminder、子目录 CLAUDE.md、路径规则、Skill 正文、MCP 完整 schema、工具调用结果和模型回复。按需进入也不等于用完立刻从会话历史中消失。

## 09 工具输出防爆炸（3 分钟）—— NEW

这页专门回答：工具输出太长怎么办、太短怎么办、怎么防止它把上下文塞爆。先澄清，这不是要求人去手工管理每次工具调用。

模型 / Agent 负责选择工具、生成带范围或分页的参数，并判断结果够不够；Claude Code 运行时负责执行、规范化结果、标记完整 / 空 / 截断状态，并保留可回查的证据引用。结果不够，模型再缩小范围续查；结果足够就停止调用。

太长：截断 + 留引用；太短或为空：给明确信号，不能让模型误判工具没跑；重复日志去重合并；二进制不塞原始字节。第 07 页橙色 38% 指的就是这类动态回填，这一关比后面的压缩更早、更便宜。

## 10 压缩机制：两类做法 + 一个触发器（4 分钟）—— 全场重点，现场点两个模式

这是我今天最想讲清楚的一页，慢一点。先纠正一个我自己以前的糊涂说法：这不是笼统的"四层"，翻源码其实是**两类压法 + 一个触发器**，而且只有一类会调模型。

左边三块看清楚。第一类**微压缩 microcompact**：把旧的工具结果就地去重、截断、换成引用（只对 Read/Bash/Grep/Glob/Web/Edit/Write 这几类结果动手），太老的直接清掉，缓存版还会走 cache_edits 在服务端删、同时保住缓存命中——**全程本地、不调模型，这是最常发生的**。第二类**全量压缩 compact**：**才调一次模型**，把旧历史压成一段摘要 + 打个边界标记 + **保留最近几轮原文** + 压完再回填关键文件（有 token 预算）——**较少触发**。第三块 **autoCompact 不是一种压法，是触发器**：盯着有效窗口减 13k 的阈值、时间、还有请求太长被拒(PTL)，来决定"什么时候压"，策略是先微压、压完还超才升级到全量。

（点"压一步"）先看微压缩：那块构建日志里 3 条一模一样的 WARNING 合并成 1 条、超长尾巴截断、大块正文换成一行引用路径——同一块结果肉眼变短，**不调模型**，占用 88%→52%。再点一步升级到全量压缩：**调一次模型**把旧历史压成一条摘要 + 边界，注意**最近一轮原文是原样保留的**，52%→30%。

关于大家常问的"是不是有两个调模型的压缩"——其实**调模型的路径只有一条，就是全量 compact**，它本身就是"摘要旧的 + 保留最近原文"，没有独立的第二个"尾部摘要 LLM"。你可能记的是它的两种**触发方式**：主动到阈值触发，和请求真超了被拒之后补压一次(reactive)。

（再点"看动态压缩"，也是手动一步步点）上下文一轮轮往上涨、越过阈值线，autoCompact 先做**最便宜的微压缩**，把旧的大结果收成引用、占用回落，接着往下跑。关键在后面：又跑一轮、新结果再回填、又超限，可旧结果**早被微压过、本地压无可压**——这才**升级到全量压缩**，调一次模型做摘要。两个结论：一是**大多数时候只做本地微压缩，全量较少触发**；二是埋个真实的坑——全量那次是**独立的模型请求**，要是失败了，会话不会自动瘦、反而继续涨到"Prompt is too long"，这是我本地排查日志里真见过的。

## 11 聊太长会变差吗 + 实操命令（3 分钟）—— 使用者最该记住

直接回答两个使用者最关心的问题：聊太长会不会变差？我能怎么手动管？

会变差。很长的对话里，早期给的指令可能被稀释掉。自动压缩大约在接近有效窗口上限时触发——右边这段真实日志里能看到 effectiveWindow=180000，有效窗口是"你配置的值"和"模型最大窗口"里取小的那个。

所以别全交给它自动。四条命令要会用：/context 先看窗口被谁占了；/compact [关注点] 手动压一次、还能指定重点保留哪块，比等它自动压可控得多；/clear 清历史但保留 CLAUDE.md；# 随手把结论写进记忆文件。落地：重要结论用 # 写进 CLAUDE.md，要压缩优先手动 /compact 带关注点。

---
# 三、权限
---

## 12 权限不是弹窗（2 分钟）

第三个专题。先摆矛盾：老弹窗问你，烦了就直接"全放开"；可要是不问，你又不敢真让它自己跑。所以成熟的权限不是一个 yes/no 弹窗，是按风险分档：直接放、问一句、拦住、受控放宽。

强调：它判断时看的不只是这一条命令，还有工具类型、参数、路径、在哪个目录、项目规则、组织策略、当前模式、你之前的选择，一起决定。所以"允许 npm test"和"允许任意 shell"，是完全两码事的授权。这一章接下来会把这套判断拆开、亲手点给大家看。

## 13 一条命令要过哪几道关（3 分钟）

把一条命令从请求到结局的完整关卡链摊开。源码里其实是十来步，我合并成七格，顺序不能乱。开场先抛最反直觉的一条：**deny 永远在 allow 前面判**——同一条命令既命中 deny 又命中 allow，deny 赢，因为它在管线更靠前。

逐格走：先看有没有被取消；整条命令撞死 deny 规则，当场拦；命中 ask 规则，问你——这格带锁，连 bypass 都盖不过；交给具体工具自校验（Bash 拆子命令、查路径、验 sed，文件工具查路径安全），这里可能直接判死；受保护路径安全检查（.git/.claude 这类），带锁，只有 bypass 放行；到这才轮到模式起作用，bypass 直接放；命中 allow 就放；都没命中的灰区，普通模式问你、auto 交给分类器。

收尾强调那两把锁：带锁的两格是 bypassPermissions 也拦不住的，这解释了下一页为什么说 bypass 不等于无人值守。

## 14 四种模式各免问什么（3 分钟）—— 重点回答 bypass

专门回答：模式到底各免问什么，尤其 bypass 是不是真的什么都不用管。先摆清六种——Shift+Tab 默认只在 Manual、acceptEdits、Plan 三种间循环，auto 要账号达标才出现，dontAsk 只能用参数进，bypass 要显式开。

逐行讲免问范围：Manual 只免只读；acceptEdits 多免文件编辑和常见 fs 命令（mkdir/touch/rm/mv/cp/sed），但仅限工作目录内；Plan 只读且不改源码；auto 几乎全放但后台有分类器审、还有 3 连 / 20 累计的熔断降级；dontAsk 只跑预授权的、其余一律拒（连 AskUserQuestion 都拒）；bypass 几乎全放。

这页最想强调两个反直觉点。第一，**bypass 不等于无人值守**——它仍会拦四类：显式 ask 规则、组织把 connector 设成 ask 的、MCP 标了需要交互的、以及 rm -rf / 和 ~ 这种熔断（v2.1.208 起连 $() 包起来的也拦）。而且有个反转：受保护路径写入在别的模式都不自动放，偏偏 bypass 放行。第二，**acceptEdits 不是自动跑一切**，只自动放编辑类和那几个 fs 命令，其它 Bash、范围外路径、受保护路径照样问你。

## 15 我配的规则谁盖谁、何时生效（2.5 分钟）

回答：规则读哪些、谁盖谁、什么时候才真生效。左边五级优先级从高到低：企业 managed（谁也改不了）> 命令行参数 > 项目 local settings.local.json > 项目共享 settings.json > 用户 ~/.claude。最关键一句：**权限规则是合并不是覆盖**——各来源的 allow/ask/deny 全叠起来，再按 deny→ask→allow 判，所以任何一级写了 deny，别处都 allow 不回来。

右边三个坑：一，合并不覆盖，别以为高优先级会把低优先级整段盖掉；二，**工作区信任弹窗**——项目 settings.json 里的 allow 规则和 additionalDirectories 属于"给能力"，必须先点信任这个文件夹才生效，没点之前读到但不应用；deny/ask 只做限制不受此限，settings.local.json 是自己的文件一般免信任。三，热更新还是重启：权限、hook 规则改完即时生效，模式切换用 Shift+Tab 或启动参数。落脚：不确定当前生效哪条，直接 /permissions 看来源。

## 16 改一处配置，七条命令一起重算（3.5 分钟）—— 现场点，全章互动中心

这是全权限章的互动核心。左边是配置：上排切模式（Manual/acceptEdits/bypass），下排切规则集（空规则/团队常用），中间显示当前生效的 allow/ask/deny。右边七条命令，每条一个判定牌。核心玩法：**改左边任意一处，右边整列一起实时重算**——这就是"并行看所有命令在当前配置下的结局"。

现场必点几组对比：①团队规则 + Manual 下：npm test 命中 allow 放行、git push 命中 ask 要问、rm -rf / 撞熔断拦。②切到 bypass：大部分变直接放，但 git push 那条 ask 仍然要问（带锁）、rm -rf / 仍被熔断拦（带锁）——亲眼验证 bypass 不是全放。③重点看两条删除：rm -rf / 永远拦；rm build/output.js 删具体文件，空规则+Manual 要问、acceptEdits 因为是工作目录内 fs 命令直接放、bypass 也放——删具体文件问不问，取决于模式和范围，不是命令名。④echo "$(cat .env)"：看着是无害的 echo，但内联 $(cat .env) 想读密钥，团队 deny 了 Read(.env) 就连这条也拦——正好引出下一页 Bash 拆解。每张牌点开能看理由：命中哪条规则、卡在哪一步。

## 17 一条 Bash 命令是被拆开来判的（2.5 分钟）

接上页那条 echo $(cat .env) 为什么被拦。Bash 命令不是拿整行去撞规则，是先做静态校验再逐段匹配，讲四件事。

第一，剥壳：timeout/time/nice/nohup/xargs 这些包装前缀先被剥掉，拿里面真正的命令去判——想用 timeout rm -rf / 绕过？剥完照样撞熔断。第二，拆复合：&&、||、|、;、以及 $() <() 命令替换都会拆成一个个子命令，每段单独过规则，**任何一段不过整条不过**——这就是 echo $(cat .env) 被拦的原因，echo 无害但内联 cat .env 命中了 deny。第三，只读内建直接放：ls cat echo pwd head tail grep find wc which diff stat 加只读 git，不用问，但一旦和危险段复合就不算数。第四，通配符讲边界：星号可以出现在命令任意位置——Bash(ls *) 匹配 ls -la 但不匹配 lsof，空格是词边界；Bash(git * main) 星号在中间，git checkout main、git push origin main 都命中。不带星号的 Bash(ls) 只精确匹配 ls；Read/Edit 的 deny 还会顺带盖住 cat/head/tail/sed 这些等价读法。收尾：常见绕过花招基本都被这套拆解堵掉了。

## 18 "删根目录"拦得住，"删一个文件"呢（2.5 分钟）

专门回答那个具体问题。把"删除"和"路径"讲透。删除分三档：第一档硬熔断，rm -rf / 和 rm -rf ~，任何模式都拦、连 bypass 都拦，v2.1.208 起把 $()、<() 包起来的变体也一起拦——注意只熔断"注定毁灭级"，不是所有 rm。第二档删具体文件（rm build/output.js），本身不撞熔断，看模式：Manual 问、acceptEdits 因为是工作目录内 fs 命令直接放、bypass 放；你若写了 deny Bash(rm *) 就任何模式都拦。第三档删到工作目录外或受保护目录，任何非 bypass 模式都不自动放。

下半讲路径校验：所有文件操作路径先规整成绝对路径再判，还会跟符号链接较劲——allow 要求软链接和它指向的目标都匹配才放，deny 只要沾一边就拦，堵 TOCTOU。受保护目录 .git .claude(除 worktrees) .ssh .vscode .idea 等，写入只有 bypass 自动放。落脚：删除危不危险，系统看的是"目标是什么、在不在范围、什么模式"，不是命令长什么样。

## 19 这轮授的权换个场景还算数吗（3 分钟）—— 使用者最该记住

专门回答大家最疑惑的：这轮授的权，换个场景还算不算数。逐格看这张表：

Bash 的"不再问"会写进项目的 settings.local.json，**同一个仓库跨会话都记得**；但文件编辑的"不再问"只当次有效，重开就没了。主代理的权限，子代理**默认继承**，可以在子代理 frontmatter 用 tools / disallowedTools / permissionMode 收窄。Hook 跑在权限判断之前，PreToolUse 能返回 allow/deny/ask，但盖不过静态硬 deny。右边是自定义：粒度能精确到命令前缀 Bash(npm test *)、目录 Edit(src/**)、甚至参数。拿不准就 /permissions 看当前生效哪条。

## 20 安全和好用不是二选一（1.5 分钟）

这页别听成纯安全。核心是分层协商：低风险只读操作走得飞快；常规项目内改动基于一次项目授权，别反复问；高影响的——改配置、访问网络、发布、删除、跨系统写入——必须拦一下、留审计、收紧范围。

产品经验很关键：权限体验一定要能解释、能只授权最小范围、能撤回。否则用户嫌烦最后一律"全放开"，你精心设计的治理就等于没有。治理的目的是让它能真正进到线上环境干活，不是把它关笼子里。

---
# 四、扩展
---

## 21 五种扩展一句话分清（2 分钟）

第四个专题。要把自己的东西接进去，第一步是选对扩展点。一句话区分：Skill = 打包的提示与流程；MCP = 接外部系统成为工具；Hook = 生命周期拦截；命令 = 可复用提示（已并入 Skill）；子代理 = 独立上下文的子任务。

选错了，可观测性、权限边界、维护成本都受影响。别把"接得多"当成功。接下来三页，分别手把手过一遍 Hook、Skill、MCP 怎么写、怎么最快接。

## 22 Hook 实操（2.5 分钟）—— 对应 deck 22~24 三页

回答"哪些生命周期点我能用，却根本不知道"。Hook 就是在流程的固定节点插一段你自己的命令，确定性执行，不靠模型判断，改完热重载不用重启——很多人不知道有这东西。

左边是常用事件：PreToolUse（工具前，能 allow/deny/ask）、PostToolUse（工具后）、UserPromptSubmit、Stop/SubagentStop、PreCompact/SessionStart。右边是能直接抄的配置：改完文件（matcher 匹配 Edit|Write）自动跑 prettier。另外两个高频场景：读到 .env 就 deny 守住密钥；跑 rm 就 ask 二次确认。

## 25 Skill 实操（2.5 分钟）

直接回答"Skill 到底怎么写、官方有没有示例、怎么最快接"。最小结构就是一个目录 + 一个 SKILL.md，目录名就是命令名。放用户级 `~/.claude/skills/` 或项目级 `.claude/skills/`（后者可提交 git）。frontmatter 里只有 description 建议必填——它决定 Claude 什么时候该用这个 skill，触发场景要写清。

关键机制是渐进式披露：平时只把名字和 description 放进上下文，完整 body 只在被调用时才载入，所以长参考资料不用时几乎零成本，改完还当场生效——这正好呼应第 07 页那套注入机制。官方示例：内置的 /code-review、/loop；cookbooks 里的财务建模等；官方 marketplace 的文档处理类和 skill-creator。最快上手：mkdir 建目录、写 SKILL.md，直接可用。

## 26 MCP 实操（2 分钟）

回答"怎么最快接一个 MCP"。MCP 把知识库、工单、数据库这些外部系统，包成 Claude 能统一调用的工具。接入就一条 `claude mcp add`。

三种传输：http（远程，最推荐，支持 OAuth）、stdio（本地进程，注意 -- 后面才是启动命令）、sse（已弃用）。作用域三种：local（默认，只你自己只当前项目）、project（写进 `.mcp.json`，可提交给团队，队友首次需 trust）、user（你所有项目通用）。接完用 `/mcp` 看连接状态。要给团队共享就 `--scope project` 把 .mcp.json 提交进仓库。呼应记忆章：工具完整 schema 是按需加载的，接得多主要压的是工具定义那块。

## 27 放回真实流程（2 分钟）

把三种扩展放回"重构并验证"这一件事。开工前 Skill 加载规范和模板；做计划时 MCP 查需求、代码索引、历史缺陷；动手前 Hook 检查分支和敏感路径；改完后 Hook 跑测试收证据；交付时插件把 PR 描述、测试结果、风险按统一格式整理好。

顺序有讲究：先接高频、少歧义、可审计的，比如只读检索、代码索引、标准测试；再把外部依赖的降级和观测治理好；直接生产发布这类高影响的，最后再接。

---
# 附
---

## 28 多 Agent：先问能不能拆（2.5 分钟）

多 Agent 我只讲一个判断框架，不神化它。两种模式：派个替身去干（Fork），适合并行搜索、局部测试；一个调度带多个工人（Coordinator），适合阶段清楚、结果能拼起来的流程。

但更多 Agent 不等于更聪明。成本来自复制上下文、抢工具、结果打架、汇总失败。设计前先问三句：子任务真独立吗？输出有没有约定的结构？一个挂了能不能局部恢复？答不上，让一个 Agent 顺序做通常更靠谱。

## 29 该按什么顺序补（2.5 分钟）

把今天的内容变成行动项。P0 是项目指令和任务摘要——长任务一致性的地基；P0 还有最小权限和审计——把实验能力带进真实环境的前提；P1 是工具协议、MCP 和 Hook；P2 才是多 Agent，它解决的是吞吐，不该在基础没打好时先上。

每项都配了验收标准：新会话拿到摘要能不能无缝接手；授权是不是最小且能解释；外部工具能不能观测、能不能降级；并行是不是真比串行快、结果还更稳。一句话：先让一个 Agent 做对、做稳、做得可控，再谈规模化并行。

## 30 收尾（1.5 分钟）

回到开场。它厉害的地方，真不在某一句神秘的 Prompt，也不在多 Agent 那些名词，而在于四件事一起成立：循环让它动，记忆让它续航，权限让它可信，扩展让它接进真实工作流。

最后抛个问题一起聊：如果下个季度只能补一样，你觉得哪样最能同时提高成功率、少人工介入、又不失控？谢谢，欢迎拍砖。

---

## 备注

本分享侧重可复用的工程模式。具体的模块名、事件数量、规则顺序、自动压缩的触发阈值、各部分占比都会随版本变化——现场就说"这是这套实现呈现出来的思路和当前观测"，别把版本细节当成长期承诺。命令（/context、/compact、claude mcp add 等）和配置字段以官方文档为准。压缩相关的 effectiveWindow、摘要失败会继续增长等，来自本地真实排查日志。
