现在这个PPT吧，给我的感觉是什么呢？三点。第一点是太浅，第二点是不全，第三点是不细。

我们以第二章记忆为以这个例子来讲解一下吧，究竟应该怎么写它？首先记忆这个模块很明显有两个，第一个是我想知道它上下文被哪些东西占用了，对吧？那自然而然我就会问，有没有什么东西是每一个对话，每一次对话的开始都要进行注入的？或者说每一轮对话都要进行注入的？如果工具输出太长了怎么办？工具输出太少了怎么办？哎，是怎么做到省去这个上下文的？防止工具上下文爆炸。有哪些我们上下文是我们看得见的？有哪些上下文是我们看不见的？但是却占用很多的。每个部分上下文是如何考虑？如何进行精细化压缩？如何进行缩减呢？防止它占用过多。每个上下文占用的比例是多少？工具占用的比例是多少？工具然后每次开启的系统提示词的比例是多少？上下文是如何注入的？按需注入、按需加载、动态注入，还是怎么注入的？配置文件是加载后锁定，还是我运行时可以更改呢？配置文件它会读哪些配置文件？优先级是什么？是按什么顺序覆盖的？

很明显这些问题是我们关注的，并且能够把它分为很几个层面来写。现在的那个填充看看那个UI的动画显示，思路是对的，但是太粗太细，一点也不全，也一点也不细。我看完之后我还是一头雾水，我没法解决我上面的问题。

再拿这个压缩技术而言，我们都知道它要多层压缩，对吧？那多层压缩哪些需要大语言模型调用？哪些不需要大语言模型调用？而是Claude Code的本地就可以进行自动的处理。现在动画展示思路是完全没有问题的，但是现在动画展示就是也太粗了，就是你每一个问题和每一个工具调用，只用一行字来形容，你根本没有办法去直观看出来上下文少在哪里了。而且上下文压缩的时候，动画时间也太短。那你必须非常集中注意力，你才能发现它哪里压缩了，就很不直观。比如说工具调用的结果，对吧？可能是很长，也有可能是可能是三四行，结果压缩之后，它成一行了，或者它就是压缩成一个摘要了。比如说我进行全量压缩的时候，我所上下文我就回到一块了，对吧？那这个应该很直观的展现出来，并且能让我们看到效果。

而且压缩这一块还是动态的，还不是静态的。是我们上下文不断产生，不断累加的时候，我们才会在后台出现，对尾端进行不断的压缩。那么这个动态的过程如何展现呢？应该在动画中展现出来，所以说目前也没有做到。

就拿压缩记忆这一块的其中两个小部分，就有明显多的问题。我们很明显应该把它做的更好更细一点。

我的这段话里有一些语误，请你自行更正，不要理解错了。

按照我的要求全面进行整改，不光是记忆这一个模块，所有模块都应该做到能够回答我这些问题。